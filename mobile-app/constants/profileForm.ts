// Mirrors frontend/src/constants/profileForm.ts (and packages/shared's
// updateBioSchema max) — kept local rather than promoted to packages/shared
// since it's one plain number, not shared behavior.
export const BIO_MAX_LENGTH = 150;
