// Predefined icon sizes, paired with global.css's text scale, so an
// Ionicons `size` prop is never a one-off number picked per call site.
export const IconSizes = {
  xs: 14,
  sm: 16,
  md: 18,
  lg: 20,
  xl: 24,
  "2xl": 28,
} as const;
